# Docker Folder Guide

## Responsibility
- Local infrastructure helpers and supporting runtime scripts.

## Working Rules
- Keep service names, ports, and credentials aligned with local startup scripts and the README.
- Treat `start_duckdb_ui.py` as a local helper, not application business logic.