# Frontend CSS Folder Guide

## Responsibility
- Visual styling for the workbench UI.

## Working Rules
- Keep class names aligned with templates and JS-generated markup.
- Prefer extending existing component styles and CSS variables over one-off selectors.
- Do not rename selectors without updating matching templates, JS markup factories, and browser smokes.